# 使用国内镜像加速下载
registry=https://registry.npmmirror.com
# 将依赖提升到虚拟存储根目录下，减少依赖嵌套深度
shamefully-hoist=true

# 使用符号链接而不是硬链接（在某些 Windows 环境中更兼容）
# symlink=true

# 严格 peer 依赖检查
strict-peer-dependencies=false
# 自动安装 peer 依赖
auto-install-peers=true
